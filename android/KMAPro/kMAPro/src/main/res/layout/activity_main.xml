<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:layout_above="@+id/KMKeyboard"
    android:orientation="vertical"
    tools:context=".MainActivity">
<!--     android:background="@color/keyman_orange" -->
    <include layout="@layout/titlebar" />

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="0dp"
        android:layout_margin="10dp"
        card_view:cardCornerRadius="10dp"
        card_view:contentPadding="10dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <android.support.v7.widget.Toolbar
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:id="@+id/card_titlebar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@android:color/white"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light" >

                <ImageButton
                    android:id="@+id/overflow_button"
                    style="?android:borderlessButtonStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="?attr/actionBarSize"
                    android:layout_gravity="right"
                    android:contentDescription="@string/image_button"
                    android:src="@drawable/ic_light_action_overflow" />

            </android.support.v7.widget.Toolbar>

            <com.tavultesoft.kmea.KMTextView
                android:id="@+id/kmTextView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginTop="?attr/actionBarSize"
                android:background="@drawable/textview_bg"
                android:ems="10"
                android:gravity="top"
                android:hint="@string/textview_hint"
                android:inputType="textMultiLine|textNoSuggestions"
                android:scrollbars="vertical">

                <requestFocus />
            </com.tavultesoft.kmea.KMTextView>

        </RelativeLayout>

    </android.support.v7.widget.CardView>

    <!-- Add padding where in-app OSK will overlay. Otherwise, OSK will block the bottom of KMTextView -->
    <!-- TODO: Determine height by OSK size -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="205dp"/>
</LinearLayout>
